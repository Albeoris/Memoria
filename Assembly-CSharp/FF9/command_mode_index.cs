using System;

namespace FF9
{
    public enum command_mode_index
    {
        CMD_MODE_INSPECTION,
        CMD_MODE_SELECT_VFX,
        CMD_MODE_LOOP,
        CMD_MODE_IDLE,
        CMD_MODE_DONE
    }
}
